package com.echel.planner.backend.stats;

/**
 * Why a completed task is worth celebrating. Declaration order determines
 * display priority (first declared = shown first on the dashboard).
 */
public enum CelebrationReason {
    FOCUSED_WORK,
    HIGH_COMPLEXITY,
    LONG_RUNNING
}
