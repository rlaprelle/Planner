# Echel Planner Branding Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers-extended-cc:subagent-driven-development (recommended) or superpowers-extended-cc:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebrand the app from "Planner" to "Echel Planner" — new name, logo component, favicon, and tagline across all user-facing surfaces.

**Architecture:** Create a single `EchelLogo` React component that renders the notebook+pencil SVG at different detail levels based on a `size` prop. Use this component in the sidebar, login page, and register page. Replace the favicon with the same SVG.

**Tech Stack:** React, SVG, Tailwind CSS (existing stack — no new dependencies)

---

## File Structure

| File | Action | Responsibility |
|------|--------|----------------|
| `frontend/src/components/EchelLogo.jsx` | Create | Logo SVG component with size-responsive detail |
| `frontend/public/favicon.svg` | Replace | Notebook+pencil favicon (simplified for 16px) |
| `frontend/index.html` | Modify | Page title "Planner" → "Echel Planner" |
| `frontend/src/layouts/AppLayout.jsx` | Modify | Sidebar header: logo + "Echel Planner" |
| `frontend/src/pages/LoginPage.jsx` | Modify | Header: logo + name + tagline |
| `frontend/src/pages/RegisterPage.jsx` | Modify | Header: logo + name + tagline |
| `e2e/tests/auth.spec.ts` | Modify | Update heading text assertions |

---

### Task 1: Create EchelLogo Component

**Goal:** A reusable React component that renders the notebook+pencil logo SVG, adjusting detail level based on size.

**Files:**
- Create: `frontend/src/components/EchelLogo.jsx`

**Acceptance Criteria:**
- [ ] Renders an SVG notebook with scribble lines and angled pencil
- [ ] Accepts a `size` prop (number, defaults to 24)
- [ ] At size >= 32: three scribbles, pencil with ferrule and gold tip
- [ ] At size < 32: two scribbles, simplified pencil
- [ ] Uses the warm cocoa color palette from the spec

**Verify:** Visual inspection in sidebar and login page after Tasks 2-4

**Steps:**

- [ ] **Step 1: Create the component file**

```jsx
// frontend/src/components/EchelLogo.jsx

/**
 * Echel Planner logo — a warm cocoa notebook with scribble lines
 * and an angled pencil drawing the bottom scribble.
 *
 * Detail simplifies automatically below 32px.
 */
export function EchelLogo({ size = 24, className = '' }) {
  const showFullDetail = size >= 32

  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 64 64"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      className={className}
      aria-hidden="true"
    >
      {/* Notebook */}
      <rect
        x="8" y="8" width="34" height="46" rx="4"
        fill="#FAF3EB"
        stroke="#7A5C3A"
        strokeWidth={showFullDetail ? 2.5 : 5}
      />

      {showFullDetail ? (
        <>
          {/* Scribble 1 */}
          <path
            d="M16 21 Q19 18, 22 21 Q25 24, 28 21 Q31 18, 34 21"
            stroke="#D5C3AA"
            strokeWidth={size >= 48 ? 2 : 3}
            strokeLinecap="round"
            fill="none"
          />
          {/* Scribble 2 */}
          <path
            d="M16 29 Q18.5 26.5, 21 29 Q23.5 31.5, 26 29 Q28.5 26.5, 31 29"
            stroke="#D5C3AA"
            strokeWidth={size >= 48 ? 2 : 3}
            strokeLinecap="round"
            fill="none"
          />
          {/* Scribble 3 (active — being drawn) */}
          <path
            d="M16 37 Q19 34, 22 37 Q25 40, 28 37 Q31 34, 34 37 Q37 40, 40 37"
            stroke="#9B7B4F"
            strokeWidth={size >= 48 ? 2 : 3}
            strokeLinecap="round"
            fill="none"
          />
          {/* Pencil at ~30 degrees */}
          <g transform="translate(40, 37)">
            <g transform="rotate(30)">
              <rect x="-2.5" y="-27" width="5" height="24" rx="1"
                fill="#9B7B4F" stroke="#7A5C3A" strokeWidth={size >= 48 ? 1.2 : 1.5} />
              <rect x="-2.5" y="-27" width="5" height="4" rx="1" fill="#7A5C3A" />
              <polygon points="-2,0 3,0 0.5,3.5" fill="#D4AA3E" />
            </g>
          </g>
        </>
      ) : (
        <>
          {/* Simplified: two scribbles, heavier strokes */}
          <path
            d="M16 23 Q20 19, 24 23 Q28 27, 32 23"
            stroke="#D5C3AA"
            strokeWidth="4"
            strokeLinecap="round"
            fill="none"
          />
          <path
            d="M16 35 Q19 31, 22 35 Q25 39, 28 35 Q31 31, 34 35 Q37 39, 40 35"
            stroke="#9B7B4F"
            strokeWidth="3.5"
            strokeLinecap="round"
            fill="none"
          />
          {/* Simplified pencil */}
          <g transform="translate(40, 35)">
            <g transform="rotate(30)">
              <rect x="-3" y="-26" width="6" height="23" rx="2"
                fill="#9B7B4F" stroke="#7A5C3A" strokeWidth="2" />
              <polygon points="-2.5,0 3.5,0 0.5,4.5" fill="#D4AA3E" />
            </g>
          </g>
        </>
      )}
    </svg>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/EchelLogo.jsx
git commit -m "feat: add EchelLogo component with size-responsive detail"
```

---

### Task 2: Replace Favicon and Page Title

**Goal:** Update the browser tab to show the new logo and name.

**Files:**
- Replace: `frontend/public/favicon.svg`
- Modify: `frontend/index.html:7`

**Acceptance Criteria:**
- [ ] Favicon renders the notebook+pencil icon at 16px
- [ ] Browser tab reads "Echel Planner"

**Verify:** Open the app in a browser, check the tab icon and title

**Steps:**

- [ ] **Step 1: Replace favicon.svg**

Write a new `frontend/public/favicon.svg` with the simplified 16px variant of the logo:

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 64 64" fill="none">
  <rect x="8" y="8" width="34" height="46" rx="5" fill="#FAF3EB" stroke="#7A5C3A" stroke-width="6"/>
  <path d="M17 25 Q22 19, 27 25 Q32 31, 37 25" stroke="#D5C3AA" stroke-width="5" stroke-linecap="round" fill="none"/>
  <path d="M17 38 Q22 32, 27 38 Q32 44, 37 38" stroke="#9B7B4F" stroke-width="5" stroke-linecap="round" fill="none"/>
  <g transform="translate(37, 38)">
    <g transform="rotate(30)">
      <rect x="-3.5" y="-24" width="7" height="21" rx="2.5" fill="#9B7B4F" stroke="#7A5C3A" stroke-width="2.5"/>
      <polygon points="-3,0 4,0 0.5,5" fill="#D4AA3E"/>
    </g>
  </g>
</svg>
```

- [ ] **Step 2: Update page title in index.html**

Change line 7 of `frontend/index.html`:

```html
    <title>Echel Planner</title>
```

- [ ] **Step 3: Commit**

```bash
git add frontend/public/favicon.svg frontend/index.html
git commit -m "feat: replace favicon and page title with Echel Planner branding"
```

---

### Task 3: Brand the Sidebar Header

**Goal:** Replace the plain "Planner" text in the sidebar with the logo icon + "Echel Planner".

**Files:**
- Modify: `frontend/src/layouts/AppLayout.jsx:214-217`

**Acceptance Criteria:**
- [ ] Sidebar header shows the 24px logo icon next to "Echel Planner" text
- [ ] Layout is horizontally aligned with a gap between icon and text

**Verify:** Open the app, check the sidebar header visually

**Steps:**

- [ ] **Step 1: Add import and update sidebar header**

Add the import at the top of `AppLayout.jsx`:

```jsx
import { EchelLogo } from '@/components/EchelLogo'
```

Replace lines 214-217 (the Logo / Brand section):

```jsx
        {/* Logo / Brand */}
        <div className="px-5 py-5 border-b border-edge-subtle flex items-center gap-2.5">
          <EchelLogo size={24} />
          <span className="text-lg font-semibold text-ink-heading tracking-tight">Echel Planner</span>
        </div>
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/layouts/AppLayout.jsx
git commit -m "feat: add Echel Planner logo to sidebar header"
```

---

### Task 4: Brand the Login and Register Pages

**Goal:** Replace the generic headings on both auth pages with the logo, product name, and tagline.

**Files:**
- Modify: `frontend/src/pages/LoginPage.jsx:36-37`
- Modify: `frontend/src/pages/RegisterPage.jsx:56-57`

**Acceptance Criteria:**
- [ ] Login page shows: 36px logo + "Echel Planner" heading + tagline in italics
- [ ] Register page shows the same branded header
- [ ] Both pages maintain centered layout

**Verify:** Navigate to `/login` and `/register`, check visually

**Steps:**

- [ ] **Step 1: Update LoginPage.jsx**

Add the import at the top:

```jsx
import { EchelLogo } from '@/components/EchelLogo'
```

Replace lines 36-37 (the heading and subtitle):

```jsx
        <div className="flex items-center justify-center gap-2.5 mb-1">
          <EchelLogo size={36} />
          <h1 className="text-2xl font-bold text-ink-heading">Echel Planner</h1>
        </div>
        <p className="text-sm text-ink-muted text-center italic mb-6">
          Planning that works with your brain, not against it.
        </p>
```

- [ ] **Step 2: Update RegisterPage.jsx**

Add the import at the top:

```jsx
import { EchelLogo } from '@/components/EchelLogo'
```

Replace lines 56-57 (the heading and subtitle):

```jsx
        <div className="flex items-center justify-center gap-2.5 mb-1">
          <EchelLogo size={36} />
          <h1 className="text-2xl font-bold text-ink-heading">Echel Planner</h1>
        </div>
        <p className="text-sm text-ink-muted text-center italic mb-6">
          Planning that works with your brain, not against it.
        </p>
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/LoginPage.jsx frontend/src/pages/RegisterPage.jsx
git commit -m "feat: brand login and register pages with Echel Planner identity"
```

---

### Task 5: Update E2E Tests

**Goal:** Fix E2E assertions that reference the old heading text.

**Files:**
- Modify: `e2e/tests/auth.spec.ts:30,104`

**Acceptance Criteria:**
- [ ] Login test checks for "Echel Planner" heading instead of "Welcome back"
- [ ] Register test checks for "Echel Planner" heading instead of "Create your account"
- [ ] E2E tests pass

**Verify:** `cd e2e && npx playwright test tests/auth.spec.ts` → all pass

**Steps:**

- [ ] **Step 1: Update auth.spec.ts assertions**

Line 30 — change:
```typescript
    await expect(page.getByRole('heading', { name: 'Welcome back' })).toBeVisible()
```
to:
```typescript
    await expect(page.getByRole('heading', { name: 'Echel Planner' })).toBeVisible()
```

Line 104 — change:
```typescript
    await expect(page.getByRole('heading', { name: 'Create your account' })).toBeVisible()
```
to:
```typescript
    await expect(page.getByRole('heading', { name: 'Echel Planner' })).toBeVisible()
```

- [ ] **Step 2: Run E2E tests to verify**

```bash
cd e2e && npx playwright test tests/auth.spec.ts
```

Expected: all tests pass.

- [ ] **Step 3: Commit**

```bash
git add e2e/tests/auth.spec.ts
git commit -m "test: update auth E2E tests for Echel Planner headings"
```

---

### Task 6: Visual Spot-Check

**Goal:** Verify all branded surfaces look correct in the running app.

**Files:** None (verification only)

**Acceptance Criteria:**
- [ ] Favicon shows notebook+pencil in browser tab
- [ ] Browser tab title reads "Echel Planner"
- [ ] Sidebar shows logo icon + "Echel Planner"
- [ ] Login page shows logo + "Echel Planner" + tagline
- [ ] Register page shows logo + "Echel Planner" + tagline
- [ ] No regressions in other pages

**Verify:** Start the dev stack, navigate through all surfaces listed above

**Steps:**

- [ ] **Step 1: Start the app**

```bash
cd frontend && node dev.js start
```

- [ ] **Step 2: Check each surface**

1. Open the app URL — verify favicon and tab title
2. Navigate to `/login` — verify logo, name, tagline
3. Navigate to `/register` — verify logo, name, tagline
4. Log in — verify sidebar header shows logo + "Echel Planner"
5. Navigate between pages — verify no visual regressions
